name: Accessibility Regression Diagnostics

on:
  pull_request:

permissions:
  contents: read

jobs:
  accessibility-diagnostics:
    if: github.head_ref == 'agent/accessibility-rtl-hardening'
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: 3.44.6
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Generate Drift code
        run: dart run build_runner build --delete-conflicting-outputs

      - name: Capture targeted accessibility output
        shell: bash
        run: |
          set +e
          flutter test test/features/medication_inventory/presentation/accessibility_rtl_widget_test.dart --reporter expanded 2>&1 | tee accessibility-regression-output.txt
          status=${PIPESTATUS[0]}
          echo "$status" > accessibility-regression-status.txt
          exit 0

      - name: Upload targeted diagnostics
        uses: actions/upload-artifact@v4
        with:
          name: accessibility-regression-diagnostics
          path: |
            accessibility-regression-output.txt
            accessibility-regression-status.txt
          if-no-files-found: error
          retention-days: 1
