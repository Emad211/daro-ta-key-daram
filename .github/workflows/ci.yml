name: Accessibility Test Diagnostics

on:
  pull_request:

permissions:
  contents: read

jobs:
  targeted-test-diagnostics:
    if: github.head_ref == 'agent/accessibility-rtl-hardening'
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: 3.44.6
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Generate Drift code
        run: dart run build_runner build --delete-conflicting-outputs

      - name: Capture accessibility test output
        shell: bash
        run: |
          set +e
          flutter test test/features/medication_inventory/presentation/accessibility_rtl_widget_test.dart --reporter expanded 2>&1 | tee accessibility-test-output.txt
          status=${PIPESTATUS[0]}
          echo "$status" > accessibility-test-exit-status.txt
          exit 0

      - name: Upload accessibility test diagnostics
        uses: actions/upload-artifact@v4
        with:
          name: accessibility-test-diagnostics
          path: |
            accessibility-test-output.txt
            accessibility-test-exit-status.txt
          if-no-files-found: error
          retention-days: 1
