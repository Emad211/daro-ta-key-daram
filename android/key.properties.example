# Copy this file to android/key.properties and keep the copy untracked.
# Relative storeFile paths are resolved from the android/ directory.
storePassword=REPLACE_WITH_UPLOAD_KEYSTORE_PASSWORD
keyPassword=REPLACE_WITH_UPLOAD_KEY_PASSWORD
keyAlias=upload
storeFile=upload-keystore.jks
