# The Flutter engine and Android plugin libraries supply their own consumer rules.
# Keep this file intentionally minimal. Add a rule only after a reproducible
# release-only failure proves that reflection or generated entry points require it.
# Broad -keep rules are prohibited because they hide compatibility problems and
# defeat the release size budget.
